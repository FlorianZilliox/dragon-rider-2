.DS_Store
__pycache__/
*.pyc
# secrets : jamais dans le dépôt (public)
.env
**/.env
# autre projet de Florian (pipeline UI Sillage, gelé), pas versionné ici
sillage-ui-pipeline/
# images générées (lourdes, 2 Mo pièce) : sources locales, régénérables via pixel_artist/generer.py
art/generes/*.png
# espaces de travail des agents (outil de développement)
.claude/

# dépendances de construction (npm install) et sortie de la PWA (npm run build)
node_modules/
dist/
essais/

# références de style venant d'œuvres d'autrui : utilisées en local pour la génération, jamais publiées
art/references/externes/

# images de validation montrées à Florian (non publiées)
revue-tours/
